# spec-reader — current task

The Chief writes the active assignment here. The spec-reader reads it, processes the CEO's directive, and writes a structured extraction to `./output/<date>-<slug>.md`.

(no task assigned yet)
