node_modules/
dist/
*.log
.DS_Store
.env
.env.*

# user vaults should never be checked into the tool repo
# (anchored to root so templates/vault/ is still tracked)
/vault/
/test-vault/
/example-vault/
