# prototyper — current task

The Chief writes the active assignment here. The prototyper reads it (plus the spec-reader, backend, and frontend outputs), and produces a single self-contained HTML file at `./output/<date>-<slug>.html`.

(no task assigned yet)
